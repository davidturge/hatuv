import { experimentalStyled, TableCell } from '@material-ui/core';

export const StyledTableCell = experimentalStyled(TableCell)({
  textAlign: 'right'
});

export const StyledTableCell1 = experimentalStyled(TableCell)({
  textAlign: 'right'
});
